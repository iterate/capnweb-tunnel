{
    "name": "capnweb-tunnel-example",
    "scripts": {
        "dev": "npx srvx weather-reporter.ts",
        "dev:bun": "bun weather-reporter.ts",
        "test": "vitest run"
    },
    "dependencies": {
        "capnweb-tunnel": "workspace:*"
    },
    "devDependencies": {
        "vitest": "^4.0.0"
    }
}