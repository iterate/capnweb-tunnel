{
  "name": "capnweb-tunnel",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "vitest run test/*.test.ts",
    "test:unit": "vitest run test/worker.test.ts",
    "benchmark:startup": "node --experimental-strip-types scripts/benchmark-startup.ts",
    "benchmark:capnweb-breakdown": "node --experimental-strip-types scripts/benchmark-capnweb-breakdown.ts",
    "benchmark:chart": "node --experimental-strip-types scripts/render-startup-chart.ts",
    "cli": "node --experimental-strip-types src/cli.ts"
  },
  "dependencies": {
    "capnweb": "^0.8.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260515.1",
    "@types/node": "^25.8.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.6",
    "wrangler": "^4.92.0"
  }
}
